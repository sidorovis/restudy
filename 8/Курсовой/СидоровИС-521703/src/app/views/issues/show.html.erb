<p>
  <b><%=h @issue.title %></b><br/>
  <%=h(@issue.description).split("\n").join("<br/>") %><br/>
</p>
<p>
  Статус: 
  <b><%=h @issue.issue_status.title %></b><br>
  По проекту: 
  <b><%= link_to @issue.project.title, @issue.project %></b>
</p>

<%= link_to 'Редактировать', edit_project_issue_path(@project,@issue) %> |
<%= link_to 'Список новых требований', project_issues_path(@project) %><br/>
<%= link_to 'Страница проекта', @project %>